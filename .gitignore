# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# Data, caches, and experiment outputs (see docs/design.md 3.5).
# Anchored to the repository root so the `mulegraph/data/` package is still tracked.
# Figures and the final tables are tracked: the README shows them. Smoke and dev outputs are not.
/data/
/mlruns/
/report/tables/smoke_*
/report/tables/*_dev_*
/report/figures/smoke_*
/report/figures/*_dev_*
*.pt
*.parquet
*.npy

# Jupyter
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# uv
.venv/
.claude/worktrees/

# graphify knowledge graph (local, regenerable)
graphify-out/

# Personal Claude Code settings (machine-specific hook paths)
.claude/settings.local.json
